{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Orang list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nama</th>
                <th>No_identitas</th>
                <th>Jk</th>
                <th>Alamat</th>
                <th>Is_active</th>
                <th>Created_at</th>
                <th>Updated_at</th>
                <th>Token</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('orang_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nama }}</td>
                <td>{{ entity.noidentitas }}</td>
                <td>{{ entity.jk }}</td>
                <td>{{ entity.alamat }}</td>
                <td>{{ entity.isactive }}</td>
                <td>{% if entity.createdat %}{{ entity.createdat|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updatedat %}{{ entity.updatedat|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.token }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('orang_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('orang_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('orang_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
